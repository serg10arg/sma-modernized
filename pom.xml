<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- ============================================================
         Parent POM del monorepo sma-modernized.
         Define versiones globales y agrega todos los módulos.
         ============================================================ -->

    <groupId>com.sma</groupId>
    <artifactId>sma-modernized</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>sma-modernized</name>
    <description>Sistema de gestión de licencias y organizaciones - Microservicios con Spring Boot 3 y Spring Cloud</description>

    <!-- Módulos del monorepo (se irán agregando por etapa) -->
    <modules>
        <module>config-server</module>
        <module>eureka-server</module>
        <module>licensing-service</module>
    </modules>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Versiones del stack -->
        <spring-boot.version>3.3.4</spring-boot.version>
        <spring-cloud.version>2023.0.3</spring-cloud.version>
        <resilience4j.version>2.2.0</resilience4j.version>

        <!-- Lombok: se fija una versión compatible con el JDK 22 en uso.
             La gestionada por Spring Boot 3.3.4 (1.18.34) falla al compilar
             bajo JDK 22 con "TypeTag :: UNKNOWN". -->
        <lombok.version>1.18.40</lombok.version>
    </properties>

    <!-- Gestión centralizada de dependencias para todos los módulos -->
    <dependencyManagement>
        <dependencies>

            <!-- Spring Boot BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Cloud BOM -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Sobrescribe la versión de Lombok gestionada por el BOM de Spring Boot.
                 Una declaración directa tiene prioridad sobre los BOM importados. -->
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                    <!-- Sin spring-boot-starter-parent hay que registrar la ejecución
                         repackage explícitamente para generar el fat-JAR ejecutable. -->
                    <executions>
                        <execution>
                            <id>repackage</id>
                            <goals>
                                <goal>repackage</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <release>${java.version}</release>
                        <!-- Necesario para que Spring MVC resuelva @PathVariable/@RequestParam por reflexión -->
                        <parameters>true</parameters>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
